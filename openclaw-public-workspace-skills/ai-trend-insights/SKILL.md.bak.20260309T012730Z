---
name: ai-trend-insights
description: >
  AI产业趋势洞察 × 金融行业战略报告生成技能。面向银行/证券/保险高管场景（行长、副行长、CIO、CTO等），用于每周输出“AI趋势 + 金融战略建议”文字稿，按需生成PPT。
  触发关键词：AI趋势汇报、金融行业AI影响、大模型进展、智能体应用趋势、AI金融战略、行长汇报、高管简报、AI洞察PPT、金融科技趋势、周报、月报分析。
---

# AI金融行业趋势洞察技能（Public 定制稳定版）

## 0) 目标与原则

- 面向高管：少技术细节，多战略影响与可执行建议。
- 每次都做实时检索，不依赖旧缓存。
- 默认先给可读文字结论；只有用户明确要求PPT时才生成PPT。
- 保持与前端联动协议一致（run_id 目录 + `.done`）。

---

## 1) 运行模式（CRITICAL）

### 1.1 文字模式（默认）
当用户未明确要求“生成PPT/输出pptx/导出PPT/汇报PPT”时：
- 只返回文字稿（3-5条高密度结论 + 简短建议），不调用 pptx。

### 1.2 PPT模式（按需）
当用户明确要求PPT时：
- 先返回文字稿摘要（给前端先展示）；
- 再进入PPT生成；
- 最终回执PPT文件信息。

---

## 2) Run ID 与产物落盘（CRITICAL）

若用户消息包含 `[[OPENCLAW_RUN_ID: <run_id>]]`：
- 必须提取 `run_id`。
- 所有产物写入：
  - `/home/ubuntu/.openclaw-public/workspace/<run_id>/`

若未提供 run_id：
- 可使用工作目录临时路径，但最终仍建议写 `.done`。

### `.done` 必须写入（成功/失败都要）
路径：
- `/home/ubuntu/.openclaw-public/workspace/<run_id>/.done`

字段最少包含：
- `status`: `completed` | `failed`
- `run_id`
- `skill`: `ai-trend-insights`
- `mode`: `text` | `ppt`
- `files`: 文件名数组（PPT模式成功时包含pptx）
- `file_count`
- `summary`: 3-5条结论
- `error`（失败时）
- `timestamp`（UTC ISO8601）

---

## 3) 前端返回协议（CRITICAL）

- 必须先回文字结论（避免前端长时间转圈时无内容可看）。
- 建议两段回执：
  1) `状态：文字稿已生成` + `run_id` + `结论摘要(3-5条)`
  2) （PPT模式）`状态：PPT生成中/已完成` + `文件路径` + `页数`
- 若PPT失败：仍返回文字结论，并说明失败原因。
- 禁止长篇灌水正文；摘要控制在 300-900 中文字符。

---

## 4) 时间与数据硬约束（CRITICAL）

- 执行前确认当前日期（UTC）。
- 标题/封面/文件名年份必须使用当前年份（当前应为 2026）。
- 未经明确要求，不得使用“2025本周/今年”表述。
- 2025仅可作为历史对比，必须标注“历史对比（2025）”。

---

## 5) 实时检索要求

至少覆盖以下维度：
1. 模型能力进展（最近7天）
2. 金融应用动态（最近30天）
3. 算力与工具链（最近30天）

建议检索词示例（可按当周热点改写）：
- Claude/GPT/Gemini/Grok 最新更新 + 当周
- agentic AI enterprise deployment + 2026
- AI banking fintech deployment + 2026
- AI infrastructure GPU inference economics + 2026

### 搜索执行策略（稳定优先）
- web_search 串行执行，避免并发突发。
- 遇到 429：退避并减少查询次数。
- 中文检索不要传非法语言值；必要时省略 `search_lang`。

---

## 6) 文字稿结构（高密度 3-5页思维）

输出按四部分组织（每部分 2-4 要点）：
1. 模型能力演进（从工具到协作者）
2. 应用生态与入口变化（金融业务重构）
3. 算力与工具链（投入与架构逻辑）
4. 战略建议（优先级 + 时间轴）

每条结论尽量包含：
- 事实/数据点
- 对金融机构的影响
- 建议动作（短句可执行）

---

## 7) PPT生成规范（仅PPT模式）

## 7.1 技能与模板（CRITICAL）
- 调用本地 pptx 技能：
  - `/home/ubuntu/.openclaw-public/workspace/skills/pptx/SKILL.md`
- 固定模板（唯一允许模板）：
  - `/home/ubuntu/.openclaw-public/workspace/skills/pptx/assets/PPT模板-浅色版16-9.pptx`
- 不允许用户自定义模板路径。
- 不允许“从零自由设计主题”替代模板。
- 若模板不可读/不存在：直接报错并返回文字稿，不得改走深色/自定义风格。

## 7.2 风格（严格继承模板）
- 必须继承上传模板的版式、字体、底纹、母版样式。
- 白底：`#FFFFFF`
- 正文：深色 `#1D1D1A`（微软雅黑/Arial）
- 强调：华为红 `#C00000`
- 辅助底：浅灰 `#F4F4F4`
- 禁止：深色封面、黑底标题条、蓝金主风格、额外背景图。

## 7.3 页数
- 默认 3-5 页高密度（超出需合并）。

---

## 8) 防炸脚本规则（CRITICAL）

### 8.1 文案与脚本分离（必须）
- 所有中文文案放在 `content.json`。
- `generate.js` 只做排版逻辑与读取，不硬编码长中文句子。

目录结构（run_id目录下）：
- `content.json`
- `generate.js`
- `*.pptx`（产物）

### 8.2 语法检查（必须）
生成前执行：
- `node --check generate.js`
- 通过后再 `node generate.js`

若 `--check` 失败：
- 自动修复一次并重试；
- 再失败则终止PPT，保留文字稿并写 `.done(status=failed)`。

---

## 9) 最终质量检查

- [ ] 数据来自本次实时检索（含时间）
- [ ] 结论有证据，不空话
- [ ] 对金融业务影响明确
- [ ] 建议有优先级/时间轴
- [ ] 年份与时态正确（2026）
- [ ] `node --check` 通过
- [ ] PPT风格符合白底华为红
- [ ] `.done` 正确写入

---

## 10) 参考

- AS-IS/TO-BE模板：`references/asis-tobe-templates.md`
- 金融场景库：`references/finance-use-cases.md`
- 图表样式：`references/chart-style-guide.md`
