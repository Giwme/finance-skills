# Packaged Skills from OpenClaw Public Workspace

Source directory:

```text
/home/ubuntu/.openclaw-public/workspace/skills
```

Packaging rule:

- Copy each skill as a complete directory.
- Include `SKILL.md` and all related resources such as `references/`, `scripts/`, `templates/`, `assets/`, examples, and other files.
- Do not package `SKILL.md` alone.

Target directory in this repository:

```text
openclaw-public-workspace-skills/
```
